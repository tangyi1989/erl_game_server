经过一番讨论之后，我们决定，放弃采用《英雄远征》的架构，采用类似于明朝的架构：

服务端在第一步将会被拆分成3个部分：

网关服务器：负责客户端tcp连接的维持，对网络协议解析，并转发到相应的逻辑服务器，并接收其他逻辑
          服务器消息，向玩家客户端进行转发。
玩家服务器：负责保存角色的状态，对角色的动作进行处理。
场景服务器：负责保存和更新地图的状态，负责对玩家所看到的场景信息进行更新。

在我们实现的第一个demo中，将不会存在数据库服务器，玩家一上线，立即生成一个新角色，随即出现在地图上。
消息的时序图，参见 ./消息时序图 中的内容。